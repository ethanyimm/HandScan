/* eslint-disable no-unused-vars */
"use strict";

// Replace this stub with a custom landmark model.
// The function should accept a canvas and return:
// {
//   indexBase: { x, y },
//   indexTip: { x, y },
//   ringBase: { x, y },
//   ringTip: { x, y },
//   confidence: 0.0 - 1.0
// }
//
// Example:
// window.detectFingerLandmarks = async (canvas) => ({ ... });
